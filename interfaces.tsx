import { ReactNode } from "react";

export interface CertificateProps{
    id:number,
    title:string,
    link:string
}


export interface AnimatedSectionProps {
  children: ReactNode;
}
